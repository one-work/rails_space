<div id="tbody" class="mt-3">
  <div class="media as-boxed">
    <div class="media-content">未绑定桌台</div>
    <div class="media-right">
      <%= link_to({ controller: 'trade/admin/orders', action: 'no_desk' }, class: 'button is-info flex-direction-column') do %>
        <p>
          <span>待结算：</span>
        </p>
      <% end %>
    </div>
  </div>
  <%= yield %>
</div>
