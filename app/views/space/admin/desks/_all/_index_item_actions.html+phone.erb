<div id="tr_<%= model.id %>" class="media as-boxed">
  <div class="media-content">
    <%= yield %>
  </div>
  <div class="media-right">
    <%= link_to({ controller: 'factory/agent/productions', desk_id: model.id }, class: 'button is-success') do %>
      <span>点餐</span>
    <% end %>
    <%= link_to({ controller: 'trade/admin/desk/orders', desk_id: model.id }, class: 'button is-info') do %>
      <span>查看订单</span>
    <% end %>
  </div>
</div>
