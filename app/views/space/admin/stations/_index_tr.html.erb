<%=
  tr_tag(
    model,
    {
      href: url_for(controller: 'buildings', station_id: model.id),
      icon: 'building',
      title: t('.buildings.index.title')
    },
    {
      href: url_for(controller: 'rooms', station_id: model.id),
      icon: 'shop',
      title: t('.rooms.index.title')
    }
  ) { yield }
%>
